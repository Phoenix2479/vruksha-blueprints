export interface Customer { id: string; customer_code: string; customer_name: string; gstin: string | null; pan: string | null; contact_person: string | null; email: string | null; phone: string | null; billing_address: string | null; shipping_address: string | null; payment_terms: number; credit_limit: number; current_balance: number; is_active: boolean; }
export interface Invoice { id: string; customer_id: string; customer_name?: string; invoice_number: string; invoice_date: string; due_date: string; reference_number: string | null; subtotal: number; tax_amount: number; total_amount: number; paid_amount: number; balance_due: number; status: 'DRAFT' | 'SENT' | 'PARTIAL' | 'PAID' | 'OVERDUE' | 'CANCELLED'; }
export interface Receipt { id: string; customer_id: string; customer_name?: string; receipt_number: string; receipt_date: string; payment_method: 'CASH' | 'CHEQUE' | 'BANK_TRANSFER' | 'UPI' | 'CARD'; amount: number; reference_number: string | null; invoices_applied: Array<{ invoice_id: string; amount: number }>; }
export interface AgingReport { customer_id: string; customer_name: string; current: number; days_1_30: number; days_31_60: number; days_61_90: number; over_90: number; total: number; }
export interface ApiResponse<T> { success: boolean; data?: T; error?: { code: string; message: string }; }
