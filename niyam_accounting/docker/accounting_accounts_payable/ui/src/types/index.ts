export interface Vendor { id: string; vendor_code: string; vendor_name: string; gstin: string | null; pan: string | null; contact_person: string | null; email: string | null; phone: string | null; address: string | null; payment_terms: number; credit_limit: number; current_balance: number; is_active: boolean; }
export interface Bill { id: string; vendor_id: string; vendor_name?: string; bill_number: string; bill_date: string; due_date: string; reference_number: string | null; total_amount: number; tax_amount: number; paid_amount: number; balance_due: number; status: 'DRAFT' | 'PENDING' | 'PARTIAL' | 'PAID' | 'OVERDUE'; }
export interface Payment { id: string; vendor_id: string; vendor_name?: string; payment_number: string; payment_date: string; payment_method: 'CASH' | 'CHEQUE' | 'BANK_TRANSFER' | 'UPI'; amount: number; reference_number: string | null; bills_applied: Array<{ bill_id: string; amount: number }>; }
export interface AgingReport { vendor_id: string; vendor_name: string; current: number; days_1_30: number; days_31_60: number; days_61_90: number; over_90: number; total: number; }
export interface ApiResponse<T> { success: boolean; data?: T; error?: { code: string; message: string }; }
